from setuptools import setup, find_packages


setup(
    name='sl_common',
    version='0.1.0',
    packages=find_packages(),
    install_requires=[
        # Add any external dependencies here
    ],
)
